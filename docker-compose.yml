services:
  mongo:
    image: mongo:7
    container_name: futurecard-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - futurecard_mongo_data:/data/db

volumes:
  futurecard_mongo_data:
